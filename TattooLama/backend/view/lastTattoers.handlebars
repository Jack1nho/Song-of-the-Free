<ul class="tatuatori">
  {{#each tatuatori}}
    <li>{{name}} {{surname}}</li> 
    <p>Ranking: {{ranking_display}} </p>
    
    <a class="btn btn-primary" href="/modificaTatuatore/{{id}}">Modifica dati</a>

    <form action="/getTattoers/{{id}}?_method=DELETE" method="POST">
      <input type="hidden" name="_method" value="DELETE">
      <input type="submit" class="btn btn-danger" value="Cancella tatuatore ">
    </form>

  {{else}}
  <h4>Non c'è nessun tatuatore in archivio.</h4>

  {{/each}}
</ul>